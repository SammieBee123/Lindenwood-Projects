Sam Disbrow
3/20/2025
Linux
My project cannot update hash tables, I think this is because of a "const" issue somewhere but I can't find where. I tried updating the setter methods and making sure nothing was const, however it still fails to correctly update.
I started by creating a person class made up of a name and phone number, then took the provided AVLTree code, and made it work with my Person class, which took a few more steps than I expected. Next, I created my main.cpp with inputs and the single char commands. Once all of that was working, I created a hash table class, tested it, debugged it, and then made the commands work for it.
